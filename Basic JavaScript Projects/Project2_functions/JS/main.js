//fnction that concatenate using +=
function myFunction() {
    var example = "I'm a web developer"; //assign the var
    example += "still a long road to reach the my goal!"; //now using the += operator to add to the var
    document.getElementById("Concatenate").innerHTML = example; //using the document.getelemntbyid
}